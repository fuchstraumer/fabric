#include <type_traits>

namespace fabric
{

    template<typename T>
    struct strong_handle
    {

    };

    template<typename T>
    struct weak_handle
    {

    };

    template<typename T>
    struct raw_handle
    {

    };
    
}